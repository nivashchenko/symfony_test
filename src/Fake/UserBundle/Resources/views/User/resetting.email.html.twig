{# src/Acme/DemoBundle/Resources/views/User/resetting.email.html.twig #}

{% block subject %}
    {{ 'mail.title_reset'|trans({}, 'FakeUserBundle') }}
{% endblock %}

{% block body_text %}
{% autoescape false %}
{{ 'mail.hello'|trans({}, 'FakeUserBundle') }} {{ user.username }} !

{{ 'mail.content'|trans({}, 'FakeUserBundle') }} {{ confirmationUrl }}

{{ 'mail.greetings'|trans({}, 'FakeUserBundle') }},
{{ 'mail.the'|trans({}, 'FakeUserBundle') }} Fake Team
{% endautoescape %}
{% endblock %}

{% block body_html %}
{#
    You can of course render the html directly here.
    Including a template as done here allows keeping things DRY by using
    the template inheritance in it
#}
{#{% include 'AcmeDemoBundle:User:resetting_email.html.twig' %}#}
{% endblock %}